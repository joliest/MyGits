package com.tracker.DatabaseHelper;

import java.sql.*;

import com.tracker.Module;

public class AttributeDatabaseManager extends DatabaseManager{
	
	private Attribute attribute;
	
	private final String UPDATE_SCRIPT = "UPDATE ATTRIBUTE VALUES(?,?,?,?,?)";

	public AttributeDatabaseManager(Module module) {
		super(module);
		
	}

	@Override
	public void add() {
		/*
		 (	"ID" NUMBER, 
		 	"GROUPID" NUMBER,
  			"NAME" VARCHAR2(50), 
  			"DATATYPE" VARCHAR2(20), 
  			"LENGTH" NUMBER)

		 */
		try {
			PreparedStatement stmt = connection.prepareStatement(UPDATE_SCRIPT);
			stmt.setInt(1, 0);
			stmt.setInt(2, 0);
			stmt.setString(3, "Name");
			stmt.setString(4, "DataType");
			stmt.setInt(5, 0);
			stmt.execute();
		} catch (SQLException sEx) {
			System.out.println(sEx);
		}
	}

	@Override
	public void update() {
		
	}

	@Override
	public void remove() {
		
	}

	
}
