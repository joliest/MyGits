<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joliver_pc.broadcastintent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".ReceiveBroadcast"
            android:enabled="true"
            android:exported="true">
            <!-- TODO: Add the lines below -->
            <intent-filter>
                <!-- TODO: Tells the program that you should only listen to ph.cormant.SendBroadcast -->
                <action android:name="com.example.joliver_pc.broadcastintent"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>